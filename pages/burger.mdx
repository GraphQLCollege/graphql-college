import GraphQLBurger from "../components/GraphQLBurger";
import FullWidth from "../components/FullWidth";
import BookBanner from "../components/BookBanner";

# Build your own burger 🍔 using GraphQL

<FullWidth>
  <GraphQLBurger defaultQuery={`{
    burger {
      bun(type: simple)
      patty(type: tripleMeat)
      topping {
        cheese(type: simple)
        egg
        lettuce
        tomato
        bacon
      }
    }
  }
  `} />
</FullWidth>

<hr
  style={{
    backgroundColor: "#e535ab",
    marginBottom: "1.4rem",
    marginTop: "1.4rem",
    height: 1,
    border: "none"
  }}
/>

<BookBanner />
